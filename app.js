function namesdom() {
    let pronoun = ['the', 'our'];
    let adj = ['great', 'big'];
    let noun = ['jogger', 'racoon'];

for (let index = 0; index < pronoun.length; index++) {
    for (let i = 0; i < adj.length; i++)
    for (let in = 0; in < noun.length; in+
)
}
}

